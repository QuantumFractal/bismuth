""" Algae life form """
import forest

class cluster()
    def __init__(self, seed, bounds):
        self.seed = seed
        self.bounds = bounds
        self.cells = set()
        self.cells.add(seed)
        self.kd_tree = forest.kdNode(seed.position)

    def self.reset(self):
        self.cells = set()
        self.cells.add(self.seed)
        self.kd_tree = forest.kdNode(self.seed.position)

    def add_cell(position, size):
        new_cell = 
        self.cells.add()

    self.draw(self, ctx):
        pass

class cell()
    def __init__(self, position=(0,0), size=10):
        self.positon = position
        self.size = size